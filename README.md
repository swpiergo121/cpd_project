# cpd_project
Use make to compile.

There are examples in the examples directory.

To enable more complex inputs, there is a parser for a json file. Tested with 1 depth level. I don't remember if it worked for more.
